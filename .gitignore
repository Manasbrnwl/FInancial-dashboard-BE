node_modules/
dist/
./package-lock.json
.env
/src/generated/prisma
*.md

market-dashboard/

# Cron job history (temporary data)
history.json

# Scratch dirs used by scripts (auto-recreated, e.g. fetch-historical-symbols.ts)
scratch/

# MCP OAuth token store (holds live access/refresh tokens)
.mcp-token-store.json